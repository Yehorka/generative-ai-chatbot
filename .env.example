# Django configuration
DJANGO_SECRET_KEY=django-insecure-h4^^*i&&c$*r!wql29$prt#u5du4&+pz2!g#zq+94ju7_h7mzy
DJANGO_DEBUG=False
# Comma-separated hosts (no spaces) that are allowed to serve the app
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database path (mounted volume in docker-compose)
SQLITE_PATH=/data/db.sqlite3

# Static files configuration shared with nginx
STATIC_URL=/static/
STATIC_ROOT=/app/staticfiles

# Optional LLM provider keys
OPENAI_API_KEY=
GEMINI_API_KEY=
MISTRAL_API_KEY=
